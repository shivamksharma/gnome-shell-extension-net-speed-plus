export const UNIT_MODE = {
    AUTO: 0,
    KB: 1,
    MB: 2,
};

export const SETTINGS = {
    UPDATE_INTERVAL: 'update-interval',
    UNIT_MODE: 'unit-mode',
    SHOW_DOWNLOAD: 'show-download',
    SHOW_UPLOAD: 'show-upload',
};

export const UPDATE_INTERVAL_MIN = 0.5;
export const UPDATE_INTERVAL_MAX = 10.0;

export const PANEL_ROLE = 'netspeed-indicator';
export const PANEL_NAME = 'Net Speed Plus';

export const BYTES_PER_KIB = 1024;
export const BYTES_PER_MIB = 1024 * 1024;
export const BYTES_PER_GIB = 1024 * 1024 * 1024;
